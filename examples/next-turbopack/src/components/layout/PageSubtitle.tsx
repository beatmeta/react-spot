export function PageSubtitle() {
  return (
    <p className="page-subtitle">
      Alt-click nested elements to verify which component owns each DOM node across
      server and client boundaries.
    </p>
  );
}
